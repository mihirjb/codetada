<!DOCTYPE html>
<html>
<head>
  <title>Codetada</title>
  <%= stylesheet_link_tag    "application", "custom", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
</head>
<body>
<header>	<div id="nav">
			<% if user_signed_in? %>
		<%= link_to "View all Tasks", root_path %>
		<span style="float:right">Signed in as <%= current_user.email %> | <%= link_to "Sign out", destroy_user_session_path, :method => 'delete' %></span>
		<% else %>
		<%= link_to "Sign in", new_user_session_path %> Or <%= link_to "Sign Up", new_user_registration_path %>
	<% end %>	
	</div>
	</header>
	<div class="container">
		<div class="success">
			<h1 class="success">
		<%= flash[:notice] %></h1>
		</div>
	<%= yield %>
</div>

</body>
</html>
